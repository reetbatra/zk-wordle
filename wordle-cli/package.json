{
  "name": "@zk-wordle/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && tsc --project tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "play": "node --no-warnings dist/preprod.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@zk-wordle/contract": "*"
  }
}